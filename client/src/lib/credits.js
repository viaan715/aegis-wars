// Mirrors server/src/credits.js — for display only; the server enforces the real costs.
export const CREDIT_COSTS = {
  createForm: 5,
  duplicateForm: 5,
  collectResponse: 1,
  generateForm: 5,
  improveQuestion: 1,
};
